# SmartCity Vision runtime dependencies.

ultralytics>=8.3.0        # YOLOv8 detection/tracking; pulls in torch + torchvision
lap>=0.5.12               # linear assignment for ByteTrack/BoT-SORT
opencv-python>=4.10.0     # video I/O, geometry, drawing
numpy>=1.26.0
pandas>=2.2.0             # analytics/reporting
pydantic>=2.8.0           # validated configuration models
PyYAML>=6.0.1             # YAML config files
fastapi>=0.115.0
uvicorn>=0.30.0
prometheus-client>=0.20.0
mlflow>=2.14.0
onnxruntime>=1.18.0
# SQLAlchemy is optional; persistence uses the stdlib sqlite3 driver.
